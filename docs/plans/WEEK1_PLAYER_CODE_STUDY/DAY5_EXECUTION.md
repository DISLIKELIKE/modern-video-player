# Day 5 执行版（功能链路与交互入口专项）

日期：2026-03-14  
目标：搞清“一个功能从输入到生效”的完整链路，建立功能映射表。

---

## 1. 今日执行安排（8 小时）

1. `09:00-09:30` 确认范围：字幕、播放列表、热键、AB Repeat、截图、章节跳转。
2. `09:30-10:30` 从 `main.cpp` 与 `VideoPlayer` 看功能入口与参数解析。
3. `10:30-11:20` 看 `pumpEvents()`，梳理 UI/热键事件如何落到 Core 接口。
4. `11:20-12:00` 看字幕链路：加载、时间线匹配、渲染器更新。
5. `13:30-14:20` 看播放列表链路：下一项/上一项/EOF 自动切换。
6. `14:20-15:00` 看截图/AB Repeat/章节跳转链路与状态更新。
7. `15:00-16:00` 做一次“功能入口 -> 核心函数 -> 数据状态变化”对照表。
8. `16:00-17:20` 输出 Day5 结论与风险清单（哪些功能已完整，哪些仅基础设施）。

---

## 2. 明日必须产出

1. 一张“功能映射表”（至少 12 个功能项）。
2. 一张“事件入口总图”（键盘/鼠标/CLI -> Core API）。
3. 一份“功能完成度分级表”（可直接给终端用户 / 仅开发验收）。

---

## 3. 重点代码定位（功能专题）

- `src/main.cpp`
- `src/video_player.cpp`
- `src/core/player_core.cpp`（`pumpEvents`、字幕、章节、截图、AB Repeat）
- `src/playlist/playlist_manager.cpp`
- `src/subtitle/srt_parser.cpp`
- `src/subtitle/subtitle_timeline.cpp`
- `src/input/hotkey_manager.cpp`

---

## 4. Day5 验收标准

- 能说明每个核心功能的调用起点和落点。
- 能区分“用户可直接用”与“仅验收能力”的边界。
- 能给出一份可执行的功能回归最短路径。
