# Day19 ???? queue generation ? `SchedulerControlSnapshot` ?? flush ???????

???2026-03-20  
???`include/thread_safe_queue.h`?`include/core/frame_queue.h`?`include/core/scheduler.h`?`src/core/scheduler.cpp`?`include/core/player_core.h`?`src/core/player_core.cpp`?`src/main.cpp`

## implementation planner

1. ??????????? item-level serial ? `Ended` ????????????????? queue flush ??? scheduler ???
2. ???? `ThreadSafeQueue / FrameQueue` ? clear/flush ?????????????? generation/epoch ????????
3. ?? scheduler ??? `running_ / paused_`?? `Seeking / Flushing / Stopping / Ended` ??????????????
4. ? queue ?? generation?? flush/clear ??????????????? producer/consumer ???????????????
5. ? scheduler ???? `SchedulerControlSnapshot`?? decode/render loop ?????????????????????????????
6. ? generation ???? diagnostics / ??????????? Debug ???

## ??????

### 1. item-level serial ??????? queue ???????????

??????? packet/frame ????????? serial ???????????????????????? queue ???????? generation/epoch?

- `ThreadSafeQueue` ? `clear()` ???????
- `FrameQueue` ? `flush()` ????????
- ?? `push()` ?????? producer???? `pop()` ?????? consumer?????????????????

??? flush ??????????? serial ? timeout ?????????????????????????????????

### 2. scheduler ????? `running_ / paused_`

???????scheduler ??????????????????????????

- `RunState`
- `PipelinePhase`
- accepted timeline serial
- `Ended`

????? scheduler ????? `running_ / paused_`??????????

- ? `Seeking / Flushing / Stopping / Ended` ??decode/render loop ??????????
- render loop ??????????? callback ???? serial ?????????

?????????????????????????? scheduler ??????????????

### 3. render wait ???????????

scheduler render loop ???????????? master clock ???????????????????? seek/flush/stopping????????

- `pause()`
- `running_`
- callback ?? serial ??

???????????????? render loop ????????????????????? render??? accepted serial ???????

## ????

### 1. `ThreadSafeQueue` / `FrameQueue` ?? generation

????? queue ???????????????????? generation?

- `ThreadSafeQueue::generation()`
- `FrameQueue::generation()`
- `FrameQueueStats::generation`

???

- `ThreadSafeQueue::clear()` ??? generation
- `FrameQueue::flush()` ??? generation
- `push()/pop()` ???????? `observed_generation`
- ?????? generation ????????????? `false`

?? flush/clear ????????????????????????????????????

### 2. ???? `SchedulerControlSnapshot`

??? `scheduler.h` ?????

```cpp
enum class SchedulerRunState { ... };
enum class SchedulerPipelinePhase { ... };

struct SchedulerControlSnapshot {
    SchedulerRunState run_state;
    SchedulerPipelinePhase pipeline_phase;
    TimelineSerial accepted_timeline_serial;
};
```

??????????????????? scheduler ???????????????????????????????????? timeline?

### 3. `PlayerCore` ?? scheduler control provider

`PlayerCore` ?? `makeSchedulerControlSnapshot()`?????

- `RunState`
- `PipelinePhase`
- accepted timeline serial

??? scheduler?????? `setControlSnapshotProvider()` ???

?? scheduler ?????? `PlayerCore` ????? seek/flush/ended?????????????????????

### 4. decode/render loop ???????

#### decode loop

??/?? decode loop ???????????

- `Starting / Running` ??? `Seeking / Flushing` ?????? decode
- ?? `Seeking / Flushing / Stopping / Ended / Paused` ?????????????

#### render loop

render loop ????????? `paused_` ???????

- ??? `Starting / Running` ? phase ? `Idle / Normal / Draining` ???? render
- pop ?????? `accepted_timeline_serial`
- ?? clock wait ????????????
- ?????? state/phase ???? render?? accepted serial ???????????

?? seek/flush/stopping ???? callback ?????????????

### 5. diagnostics ?? queue generation

??? generation ??????

- `DiagnosticsSnapshot`
- ?? `maybeLogDiagnostics()` ??
- `--performance-log-check`
- `--software-video-decode-check`
- `--1080p60-check`
- `--4k-playback-check`
- `--high-bitrate-check`
- `--long-playback-check`

??????? seek/flush ?????????? packet/frame queue ???????????????flush ?????????

## ??

- queue ? `clear()/flush()` ????????????? generation??????? producer/consumer ?????
- scheduler ????????????????? `running_/paused_` ?????????
- render loop ??????????????? accepted serial?????? seek/flush/stopping ??????
- diagnostics / ???????? queue generation ??????? flush ???????

## ????????? / ?????

### 1. `SchedulerControlSnapshot` ???????

????? scheduler ????????????????????????????? clock source?ended policy?audio-master ???????????

### 2. `Stopping` ??????????????

?? scheduler ????? `Stopping`?? `PlayerCore` ?????????????????????????????????????? stopping/draining ?????????? side effect ??????? `apply*SideEffects()`?

### 3. EOF ???????????

???????

- EOF -> `Ended`
- `play()` ? `Ended` ??
- scheduler ??? `Ended` ?????

?????

- ??????????
- playlist ????
- ???? `EndedReason`
- `Ended -> Starting` ???????????
