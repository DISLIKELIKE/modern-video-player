# Day18 ???? EOF ? `Stopped` ????????? `Ended`

???2026-03-20  
???`include/core/player_core.h`?`include/core/scheduler.h`?`src/core/player_core.cpp`?`src/core/scheduler.cpp`?`src/main.cpp`

## implementation planner

1. ?????? serial ????????????????????? timeline serial ???? EOF ?????
2. ???? EOF ??????????????????? `close/reopen` serial ??? scheduler ???????
3. ? `PlayerCore` ??????? `Ended` ????? `EndedReason`?????? `PlaybackState` ?????
4. ? EOF ???? `Draining -> Ended`???????? drain ???
5. ????????????????`close()` ???? serial ??????? stale frame ??? scheduler `rendered_frames`?
6. ??? diagnostics / analysis / records????? Debug ???

## ??????

### 1. EOF ???????? `Stopped`

????????seek/stop ???????? serial ??????? EOF ????????????demux EOF ???????? stop/deferred-stop ??????? `Stopped`?????????????

- ?????? stop
- ??????/??
- ????????

? UI ?????????????????/???????????????????? `Ended`????????????? / ????? / ???? / ended reason ???????????????

### 2. EOF ?? demux/queue?????????????

?????packet queue EOF + frame queue empty??? EOF ????????????????????????????? buffer???????

- ????????
- ????????????????????
- ?????????? ended ???audio tail ???????????

????? `audio_player_->getBufferedSeconds()` ??? drain ????????????????????????

### 3. ??????????`close/reopen` ? serial ????

??????? seek/stop/deferred stop ?? serial???????????? `close()` ??? `stop()` ??? early-return?????????????????????????? worker ????????????? reopen ?????

??? seek ????????????????serial ??????????

### 4. ??????????scheduler ? stale frame ??????

??????`renderFrame()` ?? serial ???????????? scheduler ???? render callback ?????? `rendered_frames_`??????

- ?????????????
- ?????? stale frame ??????
- ???? render starvation / late drop / throughput ?????

???????????????????? present????

## ????

### 1. ???? `EndedReason`?????? `PlaybackState`

??????????????

```cpp
enum class EndedReason {
    None = 0,
    Eof
};
```

?????

- `CoreStateSnapshot`
- `DiagnosticsSnapshot`
- ???????????

?????? `UserStop / Error / PlaylistAdvance` ???????????????????? `PlaybackState` ????? UI ????

### 2. `onRenderIdle()` ?? EOF?`Draining -> Ended`

??????????????? stopping ???????????? EOF ???

1. ??? `RunState::Running` ? demux ? EOF ???? EOF ???
2. ????????? `PipelinePhase::Draining`?
3. ?????
   - video packet queue EOF ???
   - audio packet queue EOF ???
   - video frame queue ??
   - audio frame queue ??
   - audio device buffered seconds ??? 0
4. ???????? demux/audio-consumer ??????? scheduler ??????????
5. ?? pending seek??? `EndedReason::Eof`???????? `RunState::Ended`?????? `Idle`?

???? EOF ??? `deferred stop` ???? `Stopped`???????????????

### 3. `play()` ? `Ended` ???`seek()` ? `Ended` ?? `Stopped`

???????????????????????

- `play()` ????????? `Ended`???? `seek(0.0)`????? `play` ???????
- `seek()` ??? `Ended` ????????????? `Stopped`???? seek?

???????????????/??/??????????????????????????

### 4. `close()` ? stop ?????? serial

???????????????? `close()` ??????

- ????????????? stopped
- ? demux/audio consumer/???????? stop ??
- ??? timeline ????

???? `stop()` ????????? serial???? pending seek???????? worker ???????? stale serial ?????

### 5. scheduler ?????????????

??? scheduler render callback ???? `bool`?

- `true` ???????????/??
- `false` ?????????? stale??????? present

????`pumpRenderOnce()` ??? callback ?? `true` ?????

- `rendered_frames_`
- `last_render_wall_tp_`

????????? stale serial ???????? scheduler ??????

## ??

- EOF ?????? `PipelinePhase::Draining`?????????????????? `RunState::Ended`?
- ?? `PlaybackState` ???????????? UI ????
- `play()` ? `Ended` ?????????????? stop/reset ????
- ??????? `close/reopen` serial ????????
- scheduler ???????????????stale frame ??????????????

## ????????? / ?????

### 1. queue ??? epoch/generation ????

???????????????????? item-level serial???? queue-owned epoch??????? packet/frame queue ????????????????????? generation?

### 2. `SchedulerControlSnapshot` ????

????????????? scheduler ???? `RunState / PipelinePhase / timeline_serial`????????? `Stopping / Draining / Ended` ???????????? scheduler ?????????????????? `running_ / paused_`?

### 3. EOF ???????????

??????

- ?? EOF ???????? `Ended`
- `play()` ??????

???????

- ????????
- ???? / playlist advance
- ??? `EndedReason`
- `Stopping` ???????

?????????????????
